<%= form_with(model: service, local: true) do |form| %>
  <% if service.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(service.errors.count, "error") %> prohibited this service from being saved:</h2>

      <ul>
      <% service.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :ser_date %>
    <%= form.datetime_select :sr_date, id: :service_sr_date %>
  </div>

  <div class="field">
    <%= form.label :service_request %>
    <%= form.collection_select(:id, ServiceRequest.all,:id, :service_request ) %>
  </div>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
