<%= form_with(model: service_request, local: true) do |form| %>
  <% if service_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(service_request.errors.count, "error") %> prohibited this service_request from being saved:</h2>

      <ul>
      <% service_request.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :service_request %>
    <%= form.text_field :service_request, id: :service_request_service_request %>
  </div>

  <div class="field">
    <%= form.label :service_area %>
    <%= form.collection_select(:id, ServiceArea.all, :id, :service_area) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
